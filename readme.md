# Test project with using AWS RDS - PostgreSQL

In this simple project you can:
 - create a product (set `name` and `price`)
 - get all products

 Before using only add `config.yaml` in `config` folder

 Start: `go run main.go`